{
  "name": "pthhs-site-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic build and verification tooling for the PTHHS static site.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "bash scripts/build-and-verify.sh",
    "check:repository": "python3 scripts/repository_audit.py"
  },
  "devDependencies": {
    "sharp": "0.35.4"
  }
}
