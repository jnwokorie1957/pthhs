# Historical Archive

This directory preserves superseded evidence without leaving it mixed with
active release inputs.

- `seo-2026-08/` — pre-sitemap audits, decisions, and crawl conclusions.
- `production-snapshots-2026-08/` — captured production HTML used by those
  audits.
- `source-packages/` — original compressed source/research packages.

Archive files are read-only historical context. Verify the current repository
and production site before acting on any conclusion found here.
